#!/bin/sh
cd ${0%/*} || exit 1    # run from this directory
set -x

(cd src && ./Allwmake)

wmake -all applications

curl -A "OpenQBMM-1.0.0" http://bit.ly/openqbmm100 >/dev/null 2>&1

# ----------------------------------------------------------------- end-of-file
